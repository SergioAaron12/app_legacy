package com.example.legacyframeapp.ui.theme

import androidx.compose.ui.graphics.Color

// Coffee palette (per Legacy-Frames).
val PrimaryBrown = Color(0xFF8B5C2A)        // --primary-color
val SecondaryBrown = Color(0xFFA6784E)      // --secondary-color
val AccentBrown = Color(0xFFC0894A)         // --accent-color (ajustado para evitar tono anaranjado/rosado)
val DarkBrown = Color(0xFF5C3A1A)           // --dark-color (elevated surfaces)
val LightBackground = Color(0xFFFAF8F5)     // --light-bg
val TextDark = Color(0xFF2C3E50)            // --text-dark
val White = Color(0xFFFFFFFF)               // White
val ErrorRed = Color(0xFFB00020)            // Error red

// Additional colors for a professional dark mode.
// Light coffee (light theme) and dark coffee (dark theme)
val PrimaryCoffeeLight = Color(0xFFB0793E)      // Alternate light coffee (not pinkish)
val PrimaryCoffeeDark = Color(0xFF3E2814)       // Alternate dark coffee
val DarkBackground = Color(0xFF121212)          // Root background
val DarkSurface = Color(0xFF1E1E1E)             // Main surface
val DarkSurfaceVariant = Color(0xFF262626)      // Variant for lists / chips
val DarkOutline = Color(0xFF3D3D3D)             // Subtle borders and dividers
val PrimaryContainerBrown = Color(0xFF6F451A)   // Primary container
val OnPrimaryContainerBrown = Color(0xFFFFE9D3) // Text on primary container
val SecondaryContainerBrown = Color(0xFF825F34) // Secondary container
val OnSecondaryContainerBrown = Color(0xFFFADFC2) // Text on secondary container
val AccentContainerBrown = Color(0xFFC89257)    // Tertiary/accent container
val OnAccentContainerBrown = Color(0xFFFFE9D3)  // Text on accent container
val InverseSurface = Color(0xFFE6E1E5)          // For inverted states (snackbars on dark)
val InverseOnSurface = Color(0xFF1E1E1E)        // Text on inverseSurface
